---
# defaults file for git_remote_from_local
src: null
reinitialize: no
remote_name: "{{ inventory_hostname_short }}"
remote_url: "ssh://{{ ansible_user_id }}@{{ inventory_hostname }}{{ dest }}"
override_existing_remote: no
push: yes
